{
  "name": "pico-hid-mapper-doc",
  "version": "1.0.0",
  "private": true,
  "description": "pico-hid-mapper 使用文档",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build && node scripts/cdn-postbuild.mjs",
    "build:no-cdn": "vitepress build",
    "deploy:cdn": "bash scripts/deploy-cdn.sh",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "wrangler": "^4.114.0"
  }
}
